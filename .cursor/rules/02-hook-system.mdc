---
description:
globs:
alwaysApply: false
---
# Hook系统详解

本项目主要通过Hook机制来与RainMeadow原版代码交互,不直接修改原始代码。我们使用了MonoMod.RuntimeDetour库来实现动态Hook。

## Hook原理

Hook是一种通过拦截原始方法调用,替换为自己的实现的技术。在我们的项目中:

1. 我们使用反射获取原版RainMeadow中的类型和方法
2. 使用MonoMod.RuntimeDetour创建Hook拦截这些方法
3. 提供自己的方法实现,可以在调用原始方法前后添加自定义逻辑

## 主要Hook点

### 聊天界面Hook
在[ChatHudHook.cs](mdc:src/Hooks/ChatHudHook.cs)中,我们Hook了原版的ChatHud.Draw方法:
```csharp
chatHudDrawHook = new Hook(
    chatHudType.GetMethod("Draw", 
        BindingFlags.Public | BindingFlags.Instance),
    typeof(ChatHudHook).GetMethod(nameof(HookChatHudDraw))
);
```

### 聊天消息Hook
在[ChatLogManagerHook.cs](mdc:src/Hooks/ChatLogManagerHook.cs)中,我们Hook了原版的ChatLogManager.LogMessage方法:
```csharp
chatLogMessageHook = new Hook(
    logMessageMethod,
    typeof(ChatLogManagerHook).GetMethod(nameof(HookLogMessage))
);
```

### 输入控制Hook
在[Plugin.cs](mdc:src/Plugin.cs)中,我们Hook了多个输入相关的方法:
```csharp
On.Player.checkInput += Player_checkInput;
On.RWInput.PlayerUIInput_int += PlayerUIInput_int;
```

## 创建新Hook指南

要创建新的Hook,需要遵循以下步骤:

1. 使用反射获取目标类型和方法
2. 创建Hook实例,指定原始方法和替代方法
3. 在替代方法中实现自定义逻辑,并在适当的时候调用原始方法
4. 确保在不需要Hook时正确释放资源
