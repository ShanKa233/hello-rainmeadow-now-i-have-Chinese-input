---
description:
globs:
alwaysApply: false
---
# GUI系统与中文输入

本项目使用Unity的GUI系统和自定义GHUD组件来实现中文输入功能。

## GHUD系统

GHUD(Ghost HUD)是一个自定义的HUD系统,用于在游戏中创建和管理GUI元素,特别是中文输入框。

### 初始化过程

在[GHUDTest.cs](mdc:src/GHUDTest.cs)中,我们通过以下方式创建和管理GHUD实例:

```csharp
private void ManageGHUDInstance()
{
    // 检查是否在游戏内
    bool isInGame = currentProcess is RainWorldGame;
    
    // 如果在游戏内且GHUD实例不存在,则创建
    if (isInGame && (ghud == null || GHUD.Instance == null))
    {
        var ghudObject = new GameObject("GHUD");
        ghud = ghudObject.AddComponent<GHUD>();
    }
}
```

### 输入控制

为了确保中文输入在激活时不会干扰游戏控制,我们在[Plugin.cs](mdc:src/Plugin.cs)中hook了输入系统:

```csharp
private InputPackage PlayerUIInput_int(On.RWInput.orig_PlayerUIInput_int orig, int playerNumber)
{
    bool shouldLockInput = GHUD.Instance != null && GHUD.Instance.LockInput;
    if (shouldLockInput)
    {
        // 清空输入
        return new InputPackage(...);
    }
    return orig(playerNumber);
}
```

## 中文输入实现

中文输入主要通过以下步骤实现:

1. 在游戏中创建自定义输入框
2. 捕获键盘输入并进行IME处理
3. 处理输入的中文文本
4. 将处理后的文本发送到原版聊天系统

## 开发指南

开发新的GUI功能时需要注意:

1. GUI元素必须在Unity的OnGUI方法中绘制
2. 需要正确处理输入焦点和键盘事件
3. 中文输入需要考虑IME的集成
4. 确保GUI元素在游戏退出时正确清理
